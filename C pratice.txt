chapter 1
a.

	code:

#include <stdio.h>

int main() {
    
    int fahrenheit = 58;
    int temperature = (fahrenheit * 9/5) + 32 ;
    
    printf("the temperature is %d", temperature);
    return 0;
}

	output:
the temperature is 136



B.
	code:
#include <stdio.h>

int main() {
    
    int l = 10;
    int w  = 5;
    int P = (l + w) * 2;
    int A = l * w;
    int r = 6;
    int AC = 3.14 * r * r;
    int PC = 2  *3.14 * r;
    
    printf("Perimeter of rectangle %d\n", P);
    printf("Area of rectangle %d\n", A);
    printf("Area of circle %d\n", AC);
    printf("perimeter of rectangle %d\n", PC);
    return 0;
}

	Output:
Perimeter of rectangle 30
Area of rectangle 50
Area of circle 113
perimeter of rectangle 37





#include <stdio.h>
int main() {
    int num, n5, n4, n3, n2, n1;
    int rnum ;
    printf("enter the 5 numbers ");
    scanf("%d", &num);
    n5 = num % 10;
    num = num / 10;
     n4 = num % 10;
    num = num / 10;
     n3 = num % 10;
    num = num / 10;
     n2 = num % 10;
    num = num / 10;
     n1 = num % 10;
    num = num / 10;
    
    rnum = n5* 10000 + n4*1000+ n3*100+ n2*10+ n1*1;
    printf("the reverse num id %d", rnum);
    return 0;
}



enter the 5 numbers 12345
the reverse num id 54321




#include <stdio.h>
int main() {
    
    int ram = 130;
    int shyam = 200;
    int ajay = 30;
    if(ram<shyam && ram<ajay  )
        printf("Youngest age is %d", ram);
    else  if(shyam<ram && shyam< ajay)
            printf("youngest age is %d", shyam);
      
         else 
         printf("youngest age is %d", ajay);
       
    
    return 0;
}




youngest age is 30




#include<stdio.h>

int main()
{
	int a,b,c;
	printf("Enter the angles of a triangle in degrees : ");
	scanf("%d%d%d", &a,&b,&c);
	
	if(a+b+c == 180)
		printf("The triangle is valid");
	else
		printf("The triangle is not valid.");
	
	return 0;
}

Enter the angles of a triangle in degrees : 60 60 60 
The triangle is valid


#include<stdio.h>

int main()
{
    int y;
    printf("\nEnter year ");
    scanf("%d", &y);
    if(y % 400 == 0 || y%100 != 0 && y%4 == 0)
        printf("leap year");
        else
        printf("not a leap year");
	
	return 0;
}

Enter year 2028
leap year
	